{
  "name": "lehrling-edu-mvp",
  "version": "0.1.0",
  "private": true,
  "description": "Open-source Mixed-Reality apprentice learning tool for woodworking — Phase B+C+D MVP",
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "pnpm --filter @voai/lehrling-edu dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm --filter @voai/lehrling-edu lint",
    "boundary-check": "pnpm --filter @voai/lehrlings-core boundary-check"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
